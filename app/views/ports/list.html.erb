<div class="row-fluid">
  <!-- block -->
  <div class="block">
      <div class="navbar navbar-inner block-header">
          <div class="muted pull-left">港口列表</div>
      </div>
      <div class="block-content collapse in">
          <div class="span12">
                          
              <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="offer-list">
                  <thead>
                      <tr>
                          <th>港口名称</th>
                          <th>操作</th>
                      </tr>
                  </thead>
                  <tbody>
                    <% @ports.each_with_index do |port, index| %>
                        <tr class="<%= index.odd? ? "odd" : "even" %> gradeA">
                          <td><%= port.name %></td>
                          <td class="center">
                            <button class="btn btn-success btn-mini">删除</button>
                          
                          </td>
                        </tr>
                    <% end %>
                  </tbody>
              </table>
              <div class="row" style="margin-left:0px; text-align:center">
                <div class="span12">
                  <%= will_paginate @goodses %>
                </div>
              </div>
          </div>
      </div>
  </div>
  <!-- /block -->
</div>
<script>
  $(function(){
    $("ul.nav.nav-list li").removeClass("active");
    $("#port-submenu").addClass("in");
    $("#port_list").addClass("active");
  });
</script>
